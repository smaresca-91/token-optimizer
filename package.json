{
  "name": "token-optimizer",
  "version": "0.3.0",
  "description": "Post-command token tracker + compaction advisor for Claude Code & Codex CLI",
  "bin": {
    "token-optimizer": "./bin/cli.js",
    "tko-hook": "./bin/hook.js"
  },
  "type": "module",
  "scripts": {
    "build": "tsc",
    "dev": "tsx src/hook.ts",
    "install-hooks": "node bin/install.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@anthropic-ai/tokenizer": "^0.0.4",
    "gpt-tokenizer": "^2.9.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "tsx": "^4.7.0",
    "typescript": "^5.3.0"
  }
}